import { useMemo, useState } from 'react';\n\ntype Member = { id: string; name: string; role: string; birthDate: string; birthTime: string; gender: string };\n\nconst initialMembers: Member[] = [];\n\nfunction App() {\n  const [members, setMembers] = useState<Member[]>(initialMembers);\n  const [selected, setSelected] = useState<string | null>(null);\n  const [showForm, setShowForm] = useState(false);\n  const [form, setForm] = useState({ name: '', role: '아빠', birthDate: '', birthTime: '', gender: '남성' });\n\n  const selectedMember = useMemo(() => members.find(m => m.id === selected), [members, selected]);\n\n  function addMember(e: React.FormEvent) {\n    e.preventDefault();\n    if (!form.name || !form.birthDate || !form.birthTime) return;\n    const member = { ...form, id: crypto.randomUUID() };\n    setMembers(prev => [...prev, member]);\n    setSelected(member.id);\n    setShowForm(false);\n    setForm({ name: '', role: '아빠', birthDate: '', birthTime: '', gender: '남성' });\n  }\n\n  return <div className="app">\n    <header className="topbar"><div className="brand"><span className="brand-mark">☯</span><div><strong>Family Saju AI</strong><small>가족을 이해하는 새로운 방법</small></div></div><span className="version">MVP 0.1</span></header>\n    <main className="shell">\n      <section className="hero"><div><p className="eyebrow">FAMILY RELATIONSHIP</p><h1>사주를 보고,<br/><em>대화의 방법</em>을 찾습니다.</h1><p className="hero-copy">가족의 만세력을 저장하고, 두 사람의 관계와 상황을 바탕으로 AI가 실제 대화에 도움이 되는 방법을 안내합니다.</p></div><div className="hero-card"><span>다음 단계</span><strong>만세력 엔진 검증</strong><p>첨부된 기준 프로그램과 계산 결과를 대조해 정확한 만세력 데이터를 구축합니다.</p></div></section>\n\n      <section className="section"><div className="section-head"><div><p className="eyebrow">MY FAMILY</p><h2>가족 구성원</h2></div><button className="primary" onClick={() => setShowForm(true)}>＋ 가족 추가</button></div>\n        {members.length === 0 ? <div className="empty"><div className="empty-icon">👨‍👩‍👧‍👦</div><h3>아직 가족이 등록되지 않았어요</h3><p>이름, 생년월일, 태어난 시각을 입력하면 만세력 프로필을 만들 수 있습니다.</p><button className="primary" onClick={() => setShowForm(true)}>첫 가족 등록하기</button></div> : <div className="members">{members.map(m => <button className={`member ${selected === m.id ? 'active' : ''}`} key={m.id} onClick={() => setSelected(m.id)}><span className="avatar">{m.role === '아빠' ? '👨' : m.role === '엄마' ? '👩' : m.role === '딸' ? '👧' : '👦'}</span><span><b>{m.name}</b><small>{m.role} · {m.birthDate} {m.birthTime}</small></span></button>)}</div>}\n      </section>\n\n      {selectedMember && <section className="section profile"><div className="section-head"><div><p className="eyebrow">SAJU PROFILE</p><h2>{selectedMember.name}의 만세력</h2></div><span className="status">계산 엔진 연결 대기</span></div><div className="notice"><b>정확도를 우선합니다.</b><span>현재 화면은 입력·가족관리 MVP입니다. 만세력 값은 기준 프로그램의 절기, 자시, 음력/윤달 등의 규칙을 검증한 뒤 확정해서 저장합니다.</span></div><div className="saju-placeholder"><div>년주</div><div>월주</div><div>일주</div><div>시주</div></div></section>}\n\n      <section className="section guide"><p className="eyebrow">AI CONVERSATION</p><h2>가족과 어떻게 이야기할까요?</h2><p>예: “나는 아빠인데 우리 딸과 대화하려면 어떤 것들이 필요해?”</p><div className="flow"><span>① 나 선택</span><i>→</i><span>② 상대 선택</span><i>→</i><span>③ 상황 입력</span><i>→</i><strong>AI 대화 가이드</strong></div></section>\n    </main>\n\n    {showForm && <div className="modal-backdrop" onMouseDown={() => setShowForm(false)}><form className="modal" onSubmit={addMember} onMouseDown={e => e.stopPropagation()}><div className="modal-head"><div><p className="eyebrow">NEW MEMBER</p><h2>가족 등록</h2></div><button type="button" className="close" onClick={() => setShowForm(false)}>×</button></div><label>이름<input value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} placeholder="예: 홍길동" required /></label><label>가족 관계<select value={form.role} onChange={e => setForm({ ...form, role: e.target.value })}><option>아빠</option><option>엄마</option><option>아들</option><option>딸</option><option>할아버지</option><option>할머니</option><option>기타</option></select></label><div className="row"><label>생년월일<input type="date" value={form.birthDate} onChange={e => setForm({ ...form, birthDate: e.target.value })} required /></label><label>태어난 시각<input type="time" value={form.birthTime} onChange={e => setForm({ ...form, birthTime: e.target.value })} required /></label></div><label>성별<select value={form.gender} onChange={e => setForm({ ...form, gender: e.target.value })}><option>남성</option><option>여성</option></select></label><button className="primary submit">등록하고 만세력 준비</button></form></div>}\n  </div>\n}\n\nexport default App;