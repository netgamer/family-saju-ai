// Kept as a deterministic build hook for Cloudflare Pages.
// The production build must compile the same calculator code that is used in
// development; do not rewrite src/App.tsx here because that can silently
// replace fixes to the saju calculation logic.
